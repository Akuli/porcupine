on:
  push:
    branches:
      - master
  pull_request:

jobs:
  temporary-asdf:
    timeout-minutes: 10
    runs-on: macos-latest
    steps:
    - run: echo lol > foo
    - run: echo wat > bar
    - run: grep --help
    - run: grep -r -n lol .
    - run: grep -r -n wat .
