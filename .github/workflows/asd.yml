on:
  push:
    branches:
      - new-highlighter2

jobs:
  asd:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: "3.10"
    - run: pip install tree-sitter==0.20.0
    - run: python asd.py
